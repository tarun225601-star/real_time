
name: instagram_clone
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^1.1.5
  fluttertoast: ^8.0.8
  flutter_spinkit: ^5.1.1
  flutter_icons: ^1.2.0
  flutter_secure_storage: ^5.0.2
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
