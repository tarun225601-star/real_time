import 'package:flutter/material.dart';import 'package:your_app_name/models/app_model.dart';class DetailScreen extends StatelessWidget {  final AppModel appModel;  const DetailScreen({Key? key, required this.appModel}) : super(key: key);  @override  Widget build(BuildContext context) {    return Scaffold(      appBar: AppBar(        title: Text(appModel.title),      ),      body: Padding(        padding: const EdgeInsets.all(16.0),        child: Text(appModel.description),      ),    );  }}